<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beariksonstudios.automatic.pushlocal.pushlocal" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.main.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.networkdiscovery.NetworkDisoveryActivity"
            android:label="@string/title_activity_network_disovery"
            android:parentActivityName=".activities.main.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.beariksonstudios.automatic.pushlocal.pushlocal.activities.main.MainActivity" />
        </activity>
    </application>

</manifest>
